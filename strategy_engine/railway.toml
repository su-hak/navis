[build]
builder = "NIXPACKS"

[deploy]
startCommand = "sh -c 'uvicorn strategy_engine.api.strategy_api:app --host 0.0.0.0 --port $PORT'"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
