# 11. 테스트 전략

> **목적**: 실제 DB 테스트, E2E 테스트, 단위 테스트

---

## ⚠️ 핵심 원칙: Mock 데이터 절대 금지

```typescript
// ❌ 나쁜 예: Mock Repository 사용
const mockUserRepository = {
  findOne: jest.fn().mockResolvedValue({ id: '123' })
};

// ✅ 좋은 예: 실제 DB 연결
beforeEach(async () => {
  const moduleFixture = await Test.createTestingModule({
    imports: [AppModule], // 실제 DB 설정 포함
  }).compile();
  app = moduleFixture.createNestApplication();
  await app.init();
});
```

**이유**: Mock은 실제 환경에서 발생하는 문제를 숨김

---

## 🧪 E2E 테스트 (권장)

### test/app.e2e-spec.ts
```typescript
import { Test, TestingModule } from '@nestjs/testing';
import { INestApplication } from '@nestjs/common';
import request from 'supertest';
import { AppModule } from './../src/app.module';

describe('AppController (e2e)', () => {
  let app: INestApplication;

  beforeEach(async () => {
    const moduleFixture: TestingModule = await Test.createTestingModule({
      imports: [AppModule], // 실제 AppModule 사용
    }).compile();

    app = moduleFixture.createNestApplication();
    await app.init();
  });

  afterAll(async () => {
    await app.close();
  });

  it('/ (GET)', () => {
    return request(app.getHttpServer())
      .get('/')
      .expect(200)
      .expect('Hello World!');
  });

  describe('Auth', () => {
    it('/api/auth/register (POST)', () => {
      return request(app.getHttpServer())
        .post('/api/auth/register')
        .send({
          email: 'test@example.com',
          password: 'password123',
          name: 'Test User',
        })
        .expect(201)
        .expect((res) => {
          expect(res.body.accessToken).toBeDefined();
        });
    });

    it('/api/auth/login (POST)', async () => {
      // 먼저 회원가입
      await request(app.getHttpServer())
        .post('/api/auth/register')
        .send({
          email: 'test2@example.com',
          password: 'password123',
          name: 'Test User 2',
        });

      // 로그인
      return request(app.getHttpServer())
        .post('/api/auth/login')
        .send({
          email: 'test2@example.com',
          password: 'password123',
        })
        .expect(201)
        .expect((res) => {
          expect(res.body.accessToken).toBeDefined();
        });
    });
  });

  describe('Subscription', () => {
    let accessToken: string;

    beforeEach(async () => {
      // 회원가입 및 로그인
      const registerRes = await request(app.getHttpServer())
        .post('/api/auth/register')
        .send({
          email: `test${Date.now()}@example.com`,
          password: 'password123',
          name: 'Test User',
        });

      accessToken = registerRes.body.accessToken;
    });

    it('/api/subscription/status (GET)', () => {
      return request(app.getHttpServer())
        .get('/api/subscription/status')
        .set('Authorization', `Bearer ${accessToken}`)
        .expect(200)
        .expect((res) => {
          expect(res.body.plan).toBe('free');
        });
    });
  });
});
```

---

## 🔧 테스트 데이터 정리

### 방법 1: afterEach로 정리
```typescript
afterEach(async () => {
  // 테스트 데이터 삭제
  await app.get(Connection).query('DELETE FROM users WHERE email LIKE "test%"');
  await app.get(Connection).query('DELETE FROM payments');
});
```

### 방법 2: 테스트용 DB 사용
```bash
# .env.test
DB_DATABASE=myapp_test
```

```typescript
// test/jest-e2e.json
{
  "moduleFileExtensions": ["js", "json", "ts"],
  "rootDir": ".",
  "testEnvironment": "node",
  "testRegex": ".e2e-spec.ts$",
  "transform": {
    "^.+\\.(t|j)s$": "ts-jest"
  },
  "setupFiles": ["<rootDir>/setup-test-env.ts"]
}
```

```typescript
// test/setup-test-env.ts
process.env.DB_DATABASE = 'myapp_test';
```

---

## 🧩 단위 테스트 (실제 DB 사용)

### users.service.spec.ts
```typescript
import { Test, TestingModule } from '@nestjs/testing';
import { getRepositoryToken } from '@nestjs/typeorm';
import { Repository } from 'typeorm';
import { UsersService } from './users.service';
import { User } from '../entities/user.entity';

describe('UsersService', () => {
  let service: UsersService;
  let repository: Repository<User>;

  beforeEach(async () => {
    const module: TestingModule = await Test.createTestingModule({
      providers: [
        UsersService,
        {
          provide: getRepositoryToken(User),
          useClass: Repository, // 실제 Repository 사용
        },
      ],
    }).compile();

    service = module.get<UsersService>(UsersService);
    repository = module.get<Repository<User>>(getRepositoryToken(User));
  });

  it('should be defined', () => {
    expect(service).toBeDefined();
  });

  it('findById should return a user', async () => {
    const testUser = { id: '123', email: 'test@test.com' } as User;

    // 실제 DB에 저장
    await repository.save(testUser);

    // 조회
    const result = await service.findById('123');

    expect(result).toEqual(testUser);
  });
});
```

---

## 📝 테스트 실행

### 명령어
```bash
# E2E 테스트
npm run test:e2e

# 단위 테스트
npm run test

# 커버리지
npm run test:cov
```

### package.json 스크립트
```json
{
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:cov": "jest --coverage",
    "test:e2e": "jest --config ./test/jest-e2e.json"
  }
}
```

---

## ✅ 테스트 체크리스트

### 기본 설정
- [ ] test/app.e2e-spec.ts 파일 존재
- [ ] test/jest-e2e.json 설정
- [ ] 실제 DB 연결 확인
- [ ] 테스트용 DB 별도 사용 (권장)

### E2E 테스트 작성
- [ ] 회원가입 API 테스트
- [ ] 로그인 API 테스트
- [ ] JWT 인증이 필요한 엔드포인트 테스트
- [ ] 구독 시스템 테스트 (필요 시)
- [ ] 결제 시스템 테스트 (필요 시)

### 테스트 데이터 정리
- [ ] afterEach 또는 afterAll로 데이터 정리
- [ ] 테스트 격리 확인 (각 테스트가 독립적)

### 실행
- [ ] `npm run test:e2e` 성공
- [ ] 모든 테스트 통과

---

**이전**: [10. 성능 최적화](./10_성능_최적화.md)
**다음**: [12. 배포](./12_배포.md)

---

**마지막 업데이트**: 2026-04-03
