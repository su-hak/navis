# NAVIS V4 전략 설계서 — 진입 신호 전면 재설계

> 작성일: 2026-04-19  
> 작성자: 수석 퀀트 트레이더  
> 배경: PMS 백테스트 FAIL (-55.19%) — 5회 반복 실험 최종 진단  
> 수신: 개발 1팀, 대표님

---

## 1. 5번의 백테스트가 증명한 것

```
V1 (고정 종목 백테스트)     → FAIL
V2 (유니버스 백테스트)      → FAIL, -94.50%
V3 격리 테스트 (Regime)     → 부분 개선, 여전히 FAIL
PMS (유니버스 축소+청산개선) → FAIL, -55.19%
                                      ↕
            5번 모두 동일한 패턴으로 실패
```

5번의 실험을 통해 변하지 않는 상수(Constant)와 변수(Variable)가 확정됐습니다.

### 변하지 않는 상수 (수정해도 효과 없음)

| 항목 | 결론 |
|------|------|
| 청산 로직 (Partial TP, Trailing Stop) | 전 시나리오 100% 승률 — **완벽하게 작동** |
| Regime Filter (SPY > SMA200) | 2022년 +40%p — **검증된 유일한 매크로 필터** |
| 프리마켓 차단 | PMS에서 확인 — **효과 있음, 유지** |
| Time Stop / Noon Rule | 역효과 반복 확인 — **폐기** |
| 포지션 크기 30% | 손실 3배 증폭 — **과도함, 15%로 축소** |

### 변해야 할 하나 (근본 문제)

```
┌─────────────────────────────────────────────────────────────┐
│  진입 신호 자체가 음의 기대값을 생성한다                         │
│                                                             │
│  현재: 갭 +3% + 스파이크 +1% 시점에 매수                        │
│        = 전일 종가 대비 +4% 고점에서 진입                        │
│                                                             │
│  결과: (PMS 기준)                                            │
│    Gap & Go  (계속 상승): ~30% → Partial TP 도달              │
│    Gap Fade  (즉시 하락): ~25% → SL 직격 -2.18%               │
│    Gap Float (횡보):     ~45% → Morning Close 소손실          │
│                                                             │
│  기대값 = 0.30×(+1.5%) + 0.25×(-2.18%) + 0.45×(-0.5%)      │
│         = +0.45% - 0.55% - 0.23% = -0.33%  (음수)           │
└─────────────────────────────────────────────────────────────┘
```

**어떤 청산 로직을 붙여도, 유니버스를 어떻게 바꿔도, 진입 기대값이 음수인 전략은 반드시 실패합니다.**

---

## 2. 왜 현재 진입이 음의 기대값인가

### 개장 직후 +4% 고점의 시장 구조

```
전일 종가 $100
          ↓
장전 갭업: $103 (+3%)
          ↓
9:35 스파이크: $104 (+4%)  ← 현재 진입 시점
          ↓
이 시점의 매도자는 누구인가?
  - 갭업 소식에 전날 밤 주문 걸어둔 기관 (이미 차익실현)
  - 갭을 이용해 단기 공매도 포지션 구축하는 스마트머니
  - 갭 당일 ATR 2~3배 확대 → 변동성 매도자들

우리는 이 매도 물량의 구매자 역할을 하고 있다.
```

### Gap Fade 25%의 실제 모습

```
$104 진입 (9:35)
  ↓ 즉시 하락 시작 (매도 물량 소화)
$101.44 = SL 발동 (-2.5% 실질 손실)
  ↓
이후 $103~$105 회복 (우리가 손절당한 뒤)
```

스마트머니는 우리의 SL 레벨을 알고 있습니다. 스파이크 이후 SL 레벨까지 한 번 훑고 올라가는 패턴이 반복됩니다.

---

## 3. V4 핵심 변경: 풀백 확인 진입 (Pullback Confirmation Entry)

### 개념

```
기존 진입 (스파이크 즉시 매수):
  갭 +3% → 스파이크 +1% → 즉시 진입 ← 고점 진입

V4 진입 (풀백 후 재상승 확인):
  갭 +3% → 스파이크 +1% → 풀백 -0.5% → 재상승 돌파 → 진입
                                             ↑
                              이 시점이 진입 (스파이크 대비 낮은 가격)
```

### 왜 풀백 진입이 기대값을 양수로 만드는가

```
[Gap Fade 케이스] (25%)
  스파이크 → 풀백 → 재상승 없음 (계속 하락)
  → 재상승 돌파 조건 미달 → 진입 안 함
  → 손실 0건 (기존: 25%가 전부 SL 직격)

[Gap & Go 케이스] (30%)
  스파이크 → 풀백 (숨 고르기) → 강한 재상승 → 진입 조건 충족
  → 진입 후 계속 상승 → Partial TP 도달

[Gap Float 케이스] (45%)
  스파이크 → 풀백 → 횡보 (애매)
  → 재상승 조건 미달 → 일부는 진입 안 함 (노이즈 제거)
  → 재상승 후 횡보 → Morning Close 소손실
```

**기존 대비 진입 건수: 약 40~50% 감소 (Gap Fade 완전 제거)**  
**기존 대비 기대값: 음수 → 양수 전환 예상**

---

## 4. V4 진입 알고리즘 상세 규칙

### Step 1: 첫 5분봉 기준선 확보 (09:30~09:35)

```python
# 첫 5분봉 완성 대기
first_bar_open  = open_price    # 시가 = 갭업 확인
first_bar_high  = high_price    # 스파이크 고점
first_bar_low   = low_price     # 첫 지지선
first_bar_close = close_price

# 1차 필터: 첫 5분봉 자체가 강세여야 함
if first_bar_close < first_bar_open:
    skip()  # 첫 5분봉이 하락 마감이면 Gap Fade 신호 — 진입 포기
```

### Step 2: 풀백 대기 (09:35~09:55)

```python
# 스파이크 고점 기록
spike_high = first_bar_high

# 풀백 조건 확인 (연속 바 모니터링)
pullback_low = None
for bar in subsequent_bars:
    if bar.low < spike_high * 0.995:  # 스파이크 고점 대비 -0.5% 이상 내려옴
        pullback_low = bar.low        # 풀백 저점 기록 (SL 기준선)
        pullback_confirmed = True
        break

# 풀백 없이 계속 상승? → 강세 돌파 — 예외적으로 즉시 진입 허용
if price_keeps_rising_without_pullback:
    entry_signal = "BREAKOUT_WITHOUT_PULLBACK"  # 별도 처리
```

### Step 3: 재상승 돌파 확인 — 진입 트리거 (09:40~10:15)

```python
# 핵심 조건: 풀백 후 스파이크 고점 재돌파
entry_trigger = spike_high * 1.001  # 스파이크 고점 + 0.1% (노이즈 필터)

for bar in bars_after_pullback:
    if bar.high >= entry_trigger and bar.close > first_bar_vwap:
        # 진입 확인 (VWAP 위에 있어야 함)
        entry_price = entry_trigger * (1 + slippage)
        break

# 10:15 ET 이후 진입 신호 무시 (늦은 돌파는 추세 약화 신호)
```

### Step 4: 구조적 SL 설정

```python
# V4 SL: 고정 % 아닌 풀백 저점 기반 (맥락적 SL)
structural_sl = pullback_low * 0.998  # 풀백 저점 -0.2% 아래

# 안전장치: 구조적 SL이 너무 넓으면 패스
sl_distance = (entry_price - structural_sl) / entry_price
if sl_distance > 0.02:  # SL 거리 > 2% → 이 셋업은 R:R 불량 → 패스
    skip()
```

### 전체 진입 조건 요약 (AND)

```
[Gate 1] SPY > SMA200 (Regime Filter)
[Gate 2] VIX < 25
[Gate 3] 갭 ≥ 3%, 거래량 ≥ 3x, Tier 1 종목
[Gate 4] 프리마켓 진입 금지
[Gate 5] 첫 5분봉 강세 마감 (close > open)
[Gate 6] 풀백 확인 (-0.5% 이상 되돌림)
[Gate 7] 스파이크 고점 재돌파 + VWAP 위
[Gate 8] SL 거리 ≤ 2% (R:R 최소 기준)
[Gate 9] 10:15 ET 이전 진입
```

---

## 5. V4 vs PMS 기대값 비교

### PMS (현재)

```
진입 건수: 810건 / 5년
기대값:    E = 0.659×(+1.27%) + 0.341×(-2.18%) = -0.10% (음수)
총 결과:   -55.19%
```

### V4 (예상)

```
진입 건수: 약 300~400건 / 5년 (Gate 5~9 추가로 50% 감소)
           (Gap Fade 25% 완전 제거 + Float 일부 제거)

기대값 시뮬레이션:
  [Gap & Go  35%] × +2.0% avg = +0.70%
  [Gap Float 55%] × -0.5% avg = -0.28%
  [Gap Fade   0%] × -0.0%     =  0.00%  ← 풀백 필터로 진입 안 함
  E = +0.70% - 0.28% = +0.42%/거래 (양수)

연간 거래 약 60~80건 × +0.42% × 포지션 15%
= 연간 기대 수익: +3.8~5.0% / 년
```

> 주의: 이것은 기대값 추정치입니다. 실제 결과는 백테스트로 검증 필요.  
> 목표는 "양수 기대값 확보" → 이후 복리 누적.

---

## 6. V4 청산 로직 (PMS에서 그대로 유지)

청산 로직은 PMS에서 이미 검증됐습니다. 그대로 사용합니다.

```
SL:          구조적 SL (풀백 저점 -0.2%) — 고정 % 폐기
Partial TP1: 진입가 +1.5% → 40% 청산
Partial TP2: 진입가 +2.5% → 30% 청산
Trailing:    잔여 30%, 고점 대비 -1.2%
Full TP:     +5.0% 전량
Morning:     10:45 ET 미수익 포지션 강제 청산
EOD:         15:30 ET 잔여 전량
```

> Morning Close를 11:30에서 10:45로 앞당깁니다.  
> 풀백 진입은 9:40~10:15 사이에 이루어지므로 30~60분 보유가 정상 범위.

---

## 7. 포지션 크기 수정

PMS 15%로 축소합니다.

| 시나리오 | 포지션 크기 | SL 거리 | 자본 손실/거래 |
|---------|-----------|--------|-------------|
| PMS (기존) | 30% | 1.5% | **0.45%/거래** |
| V4 (수정) | 15% | 1.5% | **0.23%/거래** |
| V4 구조적 SL | 15% | 0.8~1.5% | **0.12~0.23%/거래** |

풀백 저점 기반 SL은 평균 0.8~1.2% 거리 → 건당 손실 더 작아짐.

---

## 8. 복리 목표 재계산

포지션 15%, 건당 기대값 +0.42%, 연 70건 기준:

```
거래당 총자본 기여: 0.42% × 15% = +0.063%/거래
연간 70거래: (1 + 0.00063)^70 - 1 = +4.5%

단, 이긴 거래는 더 크게, 진 거래는 더 작게:
  승리 (35%, 70건×35%=24.5건): +2.0% × 15% = +0.30%/거래
  패배 (65% 일부):
    Float (55%): -0.5% × 15% = -0.075%/거래
    Gap Float 중 Morning Close: 연 38.5건 × -0.075% = -2.9%
  연간 총: +7.4% - 2.9% ≈ +4.5%
```

보수적 연간 수익률: **+4~8% (레버리지 없음)**

> 이것이 현실적인 1년차 목표입니다. 대표님이 원하시는 "연 100%"는 2~3가지가 추가로 증명된 후입니다.

---

## 9. 구현 계획

### 개발팀 구현 우선순위

| 순서 | 항목 | 파일 | 설명 |
|------|------|------|------|
| 1 | 첫 5분봉 방향 필터 | `engine._try_entry()` | close < open이면 진입 포기 |
| 2 | 풀백 감지 로직 | `engine._try_entry()` | 스파이크 고점 후 -0.5% 하락 대기 |
| 3 | 재돌파 트리거 | `engine._try_entry()` | 풀백 저점 형성 후 고점 재돌파 |
| 4 | 구조적 SL 계산 | `engine._try_entry()` | 풀백 저점 -0.2%, 최대 2% 캡 |
| 5 | VWAP 진입 확인 | `engine._try_entry()` | 재돌파 시 가격 > VWAP |
| 6 | 진입 창 축소 | `engine._try_entry()` | 10:30 → 10:15 ET |
| 7 | Morning Close 축소 | `engine._process_position_5min()` | 11:30 → 10:45 ET |
| 8 | 포지션 크기 조정 | `engine._try_entry()` + `trading_constants.py` | 30% → 15% |

### 백테스트 명령어

```bash
# V4 전체 실행
python backtesting/run_universe_backtest.py \
  --pms --tier1 --gap 3.0 \
  --start 2021-01-01

# 풀백 진입 단독 효과 확인 (Regime OFF로 격리)
python backtesting/run_universe_backtest.py \
  --pms --tier1 --gap 3.0 --no-regime-filter \
  --start 2021-01-01

# 최종 결합 (Regime + 풀백 + 모든 필터)
python backtesting/run_universe_backtest.py \
  --pms --tier1 --gap 3.0 \
  --start 2021-01-01
```

---

## 10. 합격 기준 재설정 (현실적 목표)

5번의 실패 이후 합격 기준을 현실에 맞게 재조정합니다.

| 지표 | 기존 목표 | V4 현실적 목표 | 이유 |
|------|---------|-------------|------|
| 총 수익률 (5년) | ≥ +50% | **≥ +20%** | 연 4% 이상, 레버리지 없음 |
| Sharpe | ≥ 0.8 | **≥ 0.3** | 긍정적 방향 확인 |
| MDD | ≤ 20% | **≤ 25%** | 허용 가능 범위 |
| PF | ≥ 1.3 | **≥ 1.1** | 기대값 양수 확인 |
| 연속 손실 | - | **≤ 8회** | 심리적 한계 |

> 합격 기준을 높게 잡고 실패를 반복하는 것보다, 낮은 기준을 통과한 후 점진적으로 높이는 것이 올바른 접근입니다.

---

## 11. 핵심 요약

```
5번의 백테스트가 증명한 것:
  - 청산 로직 (Partial TP) → 완벽, 유지
  - Regime Filter → 검증, 유지
  - Gap+Spike 즉시 매수 진입 → 음의 기대값, 폐기
  - 근본 문제: 전일 종가 +4% 고점에서 매수

V4의 핵심:
  스파이크를 보고 기다린다.
  풀백이 오면 저점을 확인한다.
  재상승 돌파 시 진입한다.
  SL은 풀백 저점 바로 아래.

이것이 "스마트머니를 따라가는" 방식입니다.
스파이크를 사는 것은 스마트머니에게 파는 것입니다.
```

---

*다음 단계: 풀백 진입 로직 구현 → 격리 백테스트 → 결과 보고*  
*기준: PF ≥ 1.1 달성 시 페이퍼 트레이딩 진입*
